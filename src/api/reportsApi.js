// Central API layer placeholder for report-related endpoints (currently using dummy/local data)
import client from "./client";

export const getReports = () => Promise.resolve({ success: true, data: [] });
