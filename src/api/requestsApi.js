// Central API layer placeholder for request-related endpoints (currently using dummy/local data)
import client from "./client";

export const getRequests = () => Promise.resolve({ success: true, data: [] });
